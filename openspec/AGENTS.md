# OpenSpec Instructions

The OpenSpec workflow instructions for AI assistants are in `CLAUDE.md` under the **OpenSpec Instructions** section at the top of the file.

That section covers:
- When to create a proposal vs. fix directly
- Three-stage workflow (create → implement → archive)
- Directory structure
- Proposal and spec delta format
- Key CLI commands

For detailed reference material (troubleshooting, multi-capability examples, full best-practices guide), see the original content that was here — it was consolidated into CLAUDE.md to avoid requiring a separate file read during planning tasks.
