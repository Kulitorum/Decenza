## 1. Prerequisite & inventory

- [ ] 1.1 This change owns the seam (split-ugs superseded/archived — do not depend on it). Add the `ProfileExpectation` struct + `Expected*:`/diagnostic-directive parser to `ShotSummarizer::loadProfileKnowledge()`, mirroring the existing `ugs`/`ugsInferred` precedent (additive field on `ProfileKnowledge`, no call-site change). Confirm the #1160 per-profile/editor KB sections already exist in the shipped `resources/ai/profile_knowledge.md` (`## D-Flow`, `## D-Flow Q variant`, `## Damian's LRv2 / LRv3`, `## A-Flow`, `## Londinium` with `Also matches:` keying — from archived `correct-dflow-variant-ugs`); key arms to those existing sections and do NOT rename them. NOTE: the editor-vs-profile *prose/terminology* and the stale-A-Flow-name correction in this same file are owned by the separate `correct-dflow-aflow-editor-profile-docs` change — this change only *appends* `Expected*:`/arm directive lines; both changes preserve headers/aliases so they merge in either order without collision. Do not also rewrite the section prose here.
- [ ] 1.2 Use the design D9 audit (incl. D10) as the authoritative arm set — do not re-derive. Two-sided: **Adaptive v2** (`decent-guide` + `*-video` + `adaptive-adastra`, grind-tolerant note), **Londinium/LRv2-LRv3** (`decent-guide` + `dark-video`/`medium-video`), **Blooming Espresso** (`light-video`, mark `synthesis`), **D-Flow / Q** (a *profile* built with the D-Flow editor — D-Flow/A-Flow are editor types, not profiles; see design D11/D10b; keyed to its own KB section; `[SRC:profile-notes]` verbatim "grind for a pressure peak between 6 and 9 bar"; editor limit 10.0 bar > 9-target → clean **two-sided** (too coarse <~6 bar, too fine >~9 bar unconfounded); non-conditional; strongest D-Flow-editor arm) **and D-Flow / La Pavoni** (same `[SRC:profile-notes]` verbatim 6–9 goal; editor limit 9.0 bar == target ceiling → clean **one-sided too-coarse** arm only (peak never reaches ~6 bar); its too-fine direction is the editor-clamp suppression case, NOT a fabricated arm; keyed to its own KB section). D-Flow **editor-level** items (not profile arms): `PressureShape: declining` structural prior `[SRC:maintainer]`+`[SRC:dflow-author-video]`-corroborated; per-profile pressure-limit clamp `Suppress:` rule `[SRC:de1app-dflow]` (default 8.5 / Q 10.0 / La Pavoni 9.0) — replaces the deleted "capped variant" notion; single-arm: **E61** too-coarse (`dark-video`), **Allongé** too-fine + channeling-normal suppression (`light-video`), **Turbo Shot** (`eaf-profiling`, non-high confidence). Everything else: prose track only.
- [ ] 1.3 Transcribe each arm verbatim from its cited source with the `[SRC]` tag + confidence marker; mark Blooming arms `synthesis-from-first-party-prose`. Do not fabricate a missing arm to make a pair (E61/Allongé stay single-arm). Separately inventory the broad **prose-track** profiles (first-party-backed espresso profiles in `docs/PROFILE_KNOWLEDGE_BASE.md`) for §3.3 coaching prose. Invent nothing.
- [ ] 1.4 Grep `resources/ai/profile_knowledge.md` and `docs/PROFILE_KNOWLEDGE_BASE.md` to confirm the new directive prefixes (`GrindTooCoarse:`, `GrindTooFine:`, `PressureShape:`, `PreinfusionDripG:`, `FlowStability:`, `Suppress:`) collide with no existing line prefix. Capture `## ` heading-count and section-span baselines for the drift check.

## 2. Grammar & parser extension

- [ ] 2.1 Document the extended directive grammar (value = range/pattern token; arms = observable signature strings; `PressureShape` ∈ declining/flat/rising-bad/crash/pattern; `PreinfusionDripG` optional bounds; `Suppress` = `;`-delimited fact list) in a comment beside the parser and in the design's grammar note.
- [ ] 2.2 In `src/ai/shotsummarizer.h`, extend `ProfileExpectation` with optional fields for the two diagnostic arms, shape qualifier, preinfusion-dripping (value + optional too-coarse/too-fine bounds), flow-stability, and a structured suppression set — all defaulting to absent, mirroring the existing `Expected*:` field precedent.
- [ ] 2.3 In `src/ai/shotsummarizer.cpp` `loadProfileKnowledge()`, parse the new directive lines with the same prefixed-directive style. Absent line → field absent. Malformed individual value → that field absent, section parse continues, no throw. Range/pattern tokens MUST NOT be coerced to `min == max`. No call site reads the new fields.

## 3. Seed the evidence-graded arm set + broad prose

- [ ] 3.1 Add the extended directive lines (`GrindTooCoarse:`/`GrindTooFine:`/`PressureShape:`/`PreinfusionDripG:`/`FlowStability:`/`Suppress:`) only to the six D9 arm-set sections, each value verbatim from §1.3 with a per-arm `[SRC]` tag + confidence marker; mark Blooming arms `synthesis`; add Adaptive v2's grind-tolerant-by-design suppression/leniency note.
- [ ] 3.2 Leave one-sided profiles single-armed (E61 too-coarse only; Allongé too-fine only). Seed the **D-Flow / Q profile** KB section with the `[SRC:profile-notes]` two-sided peak bound (too coarse: peak <~6 bar; too fine: peak >~9 bar), and the **D-Flow / La Pavoni profile** section with a `[SRC:profile-notes]` one-sided too-coarse arm only (peak <~6 bar; NO too-fine arm — its 9.0 bar limit == target ceiling, fine direction is the clamp-suppression case). Each keyed to its own profile only. Seed the D-Flow **editor-level** items separately: `PressureShape: declining` structural prior (`[SRC:maintainer]`, video-corroborated) applicable to `D-Flow/…` profiles, and a per-profile pressure-limit clamp `Suppress:` rule (`[SRC:de1app-dflow]`: default 8.5 / Q 10.0 / La Pavoni 9.0 bar) — keyed by editor type / resolved profile, NOT a "capped variant." Do not fabricate a missing arm; do not attach a numeric arm to any `D-Flow/…` profile lacking its own cited target.
- [ ] 3.3 Add concise per-profile coaching **prose** to the broad first-party-backed set (prescriptive "expect / start here" may be generous; diagnostic phrasing stays symptom-specific) so the prose track lands even where no structured arm exists.
- [ ] 3.4 Verify no `## ` heading-count regression and that every built-in profile title still resolves to exactly one section.

## 4. Active-coaching teaching clause

- [ ] 4.1 Strengthen the `shotAnalysisSystemPrompt` "How to read structured fields" clause from passive-directional to active-coaching: on a matched `GrindTooCoarse:`/`GrindTooFine:` signature, name the signature and recommend the grind direction.
- [ ] 4.2 In the same clause, require AND-gating against the profile `Suppress:` set, a fired channeling detector, unknown/very-fresh beans, and grind-tolerant-by-design profiles before any too-coarse/too-fine statement; state taste (too sour → finer, too bitter → coarser) outranks any curve/expectation verdict; permit generous prescriptive guidance.
- [ ] 4.3 Add the soft "ran outside the profile's intended shape — judge by taste" note for the clean-but-off-guideline / no-arm-match case (still suppression-gated and taste-deferring); finalize wording against the specs delta.
- [ ] 4.4 Confirm the clause ships identically into both the in-app advisor system prompt and `dialing_get_context` via the existing KB-injection path — no separate copy.

## 5. Living-doc sync

- [ ] 5.1 Mirror the extended directive convention and every seeded value into `docs/PROFILE_KNOWLEDGE_BASE.md` beside the existing `UGS:` / `Expected*:` field-line docs; record the two-sided-diagnostic + per-profile-suppression structure so the next contributor inherits it.
- [ ] 5.2 Review `docs/UNIVERSAL_GRIND_SETTING.md`: add a one-line note that the active-coaching prose clause (directional, suppression-gated, taste-outranks) is ADR-consistent — the rejected design was a rigid LLM-facing band fed as a rule; this is neither.
- [ ] 5.3 Fix the stale `adaptive-search` source row in `docs/PROFILE_KNOWLEDGE_BASE.md` (`decentespresso.com/blog/dialing_in_the_rao_allonge_and_adaptive_v2_profiles` → HTTP 404): repoint to the live equivalent if one exists, else mark the row dead and re-tag any `[SRC:adaptive-search]` lines to their surviving first-party source (`adaptive-adastra` / `*-video` / `decent-guide`).
- [ ] 5.4 Register the new first-party source in the `## Sources` table of `docs/PROFILE_KNOWLEDGE_BASE.md`: key `dflow-author-video`, description "Damian's D-Flow Profile — Decent Espresso livestream (Damian, the D-Flow author + John Buckman), transcript at `docs/dflow_damian_profile_transcript.txt`", URL `https://www.youtube.com/watch?v=3Ib63xBNrzw`. Also add source key `de1app-dflow` (de1app `D_Flow/code.tcl` stock D-Flow profile parameters incl. per-profile pressure limits; mirrored in `docs/CLAUDE_MD/RECIPE_PROFILES.md`). Tag the editor-clamp `Suppress:` rule + per-profile limits `[SRC:de1app-dflow]`, the >9-bar-harsh taste rationale + lever-decline prior `[SRC:dflow-author-video]`/`[SRC:maintainer]`, and the D-Flow/Q peak bound `[SRC:profile-notes]`.

## 6. Tests

- [ ] 6.1 `tst_dialing_blocks`: extended parse contract — a fully-seeded section exposes both arms + shape + dripping bounds + flow-stability + suppression set; a partially-seeded section exposes only present fields; malformed degrades to absent without throwing; a pattern value round-trips without `min == max` collapse.
- [ ] 6.2 Seed coverage (D9/D10 set) — Adaptive v2 / Londinium / Blooming expose both arms (Blooming tagged `synthesis`); the D-Flow/Q *profile* exposes a `[SRC:profile-notes]` clean two-sided peak bound (≈6–9 bar, limit 10.0) keyed to its own section; D-Flow/La Pavoni exposes a `[SRC:profile-notes]` one-sided too-coarse arm only (peak <~6 bar; no too-fine arm — limit 9.0 == target ceiling); the D-Flow *editor* exposes a `PressureShape: declining` prior + a per-profile pressure-limit clamp `Suppress:` rule (`[SRC:de1app-dflow]`), with no "capped variant" and no numeric arm on uncited `D-Flow/…` profiles (incl. `D-Flow / default`); E61 and Allongé expose exactly one arm with the opposite absent; Turbo Shot's arm is `[SRC:eaf-profiling]` non-high; an uncited profile resolves to arm-absent with no fabricated value; every seeded arm carries an `[SRC]` tag + confidence marker.
- [ ] 6.3 Suppression set — a section with a `Suppress:` line exposes the structured set; a section without one exposes an empty set and parses unchanged.
- [ ] 6.4 Teaching clause — the espresso `shotAnalysisSystemPrompt` contains the active-coaching directive, the AND-gating requirements (suppression set, channeling, bean freshness, grind-tolerant), and the taste-outranks statement; the same per-profile guidance + clause is identical on the in-app and `dialing_get_context` surfaces.
- [ ] 6.5 Invariant — no synthesized confidence/quality/trust score is injected into the advisor input and no coaching verdict path writes to the shot record / DB / visualizer.coffee (assert no new write site; the only output is prose).

## 7. Verification

- [ ] 7.1 Build via Qt Creator MCP (0 errors, 0 warnings) and run the full Qt Test suite — must stay green plus the new assertions.
- [ ] 7.2 Spot-check the rendered `dialing_get_context` KB section and the in-app advisor prompt: arms present only on the D9/D10 set with `[SRC]`/confidence tags (D-Flow shape-arm `[SRC:maintainer]`), broad coaching prose present, the active-coaching/taste-deferring/suppression-gated clause present, drift-check parity between resource and `docs/PROFILE_KNOWLEDGE_BASE.md`.
- [ ] 7.3 Live sanity checks: (a) a real **D-Flow / Q** shot whose peak exceeds ~9 bar → advisor names the "peak above the stated 6–9 bar goal" too-fine signature, recommends coarser, defers to taste; peak under ~6 bar → too-coarse, grind finer. (b) a **D-Flow / La Pavoni** shot with peak under ~6 bar → too-coarse signature fires; a La Pavoni shot pegged flat at its 9.0 bar limit → the clamp `Suppress:` rule fires and NO too-fine signature is emitted (fine direction is clamp-masked, not a fault). (c) a `D-Flow / default` shot pegged flat at its 8.5 bar editor limit → clamp `Suppress:` fires (no false too-fine), no "capped variant" special-case. (d) a `D-Flow/…` profile with no cited target exposes only the `PressureShape: declining` prior, no numeric arm.
