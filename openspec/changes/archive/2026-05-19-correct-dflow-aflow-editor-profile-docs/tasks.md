## 1. Baseline & inventory

- [x] 1.1 Capture the drift-check baseline for `resources/ai/profile_knowledge.md`: the exact list of `## ` headings and every `Also matches:` line (these MUST be byte-identical after this change).
- [x] 1.2 List the real built-in profile titles from `resources/profiles/a_flow_*.json` and `d_flow_*.json` / `damian_s_*.json` (`title` field) to use as the authoritative name set. Confirm the stale names in the shipped KB (`A-Flow / medium`, `-dark`, `-very dark`, `-like D-Flow`) have no backing file.
- [x] 1.3 Grep the shipped KB D-Flow/A-Flow sections for profile-implying phrasings ("base D-Flow", "D-Flow / Damian family", "D-Flow Q variant" *in prose* (not the header), "standard D-Flow variants", "A D-Flow variant", "A-Flow variants").

## 2. Fix the shipped KB (`resources/ai/profile_knowledge.md`)

- [x] 2.1 Rewrite the `## D-Flow` section body prose to the editor model: D-Flow is an editor type; the profile is the name past the `/`; the pressurized-soak → flow-pour-with-pressure-limit → lever-decline structure and the per-profile pressure-limit clamp are editor-level behavior; "base D-Flow" → "the `D-Flow / default` profile" (the starter/example); "the whole D-Flow / Damian family" → "profiles built with the D-Flow editor". Do NOT change the heading or `Also matches:`.
- [x] 2.2 Rewrite `## D-Flow Q variant` and `## Damian's LRv2 / LRv3` body prose the same way (each is a *profile built with the D-Flow editor*; cross-reference shared editor behavior rather than calling them "variants of D-Flow"). Headers/aliases unchanged.
- [x] 2.3 Rewrite the `## A-Flow` section: A-Flow is an editor type (by Janek); replace the stale profile list with the real built-ins `A-Flow / default-light/-medium/-dark/-very-dark/-like-dflow`; describe per-profile differences as roast targeting of distinct profiles, not "A-Flow variants". Header/aliases unchanged. (No `[SRC:` citation convention exists in this KB — `Creator:` is the convention and is preserved verbatim; no synthetic citation introduced.)
- [x] 2.4 Run the §1.1 drift-check: heading list and every `Also matches:` line byte-identical; only prose/in-section names changed. Confirm `## ` heading count unchanged. (42 headings, 27 `Also matches:` lines, byte-identical incl. line numbers.)

## 3. Consistency sweep of other docs

- [x] 3.1 Edit AI/user-facing docs where wording implies D-Flow/A-Flow is a profile: `docs/CLAUDE_MD/AI_ADVISOR.md` (per-variant→per-profile), `docs/UNIVERSAL_GRIND_SETTING.md` ("D-Flow family's 7 Damian variants"→editor model). `docs/ESPRESSO_DIAL_IN_REFERENCE.md` and `docs/SIMPLE_PROFILE_EDITOR.md` had no profile-implying phrasing (already editor-consistent). No incidental churn.
- [x] 3.2 `docs/CLAUDE_MD/RECIPE_PROFILES.md` verified authoritative-correct (title-first editor selection, no Damian-standalone=D-Flow claims) — no edits. `docs/PROFILE_KNOWLEDGE_BASE.md` was NOT actually clean: it had a section "Damian's D-Flow-editor Profiles" + ~8 assertions that LM Leva/LRv2/LRv3/Q are D-Flow-editor output (opposite of the title-prefix rule). Reconciled: renamed section to "Damian's Standalone Profiles", corrected all assertions to the title-prefix model (LRv2/LRv3 standalone & distinct from each other; Damian's Q = Advanced editor, related to D-Flow/Q), mirrored the grind-table row.
- [x] 3.3 Lower-traffic docs (`AUTO_FLOW_CALIBRATION.md`, `BLE_PROTOCOL.md`, `SAW_LEARNING.md`, `MCP_SERVER.md`, `MCP_TEST_PLAN.md`, `SHOT_REVIEW.md`): reviewed all D-Flow/A-Flow mentions. None actively assert D-Flow/A-Flow is a profile — all are incidental shorthand ("13 D-Flow shots"), correct slash-names ("D-Flow/Q"), or example lists in detector docs. `MCP_TEST_PLAN.md` is a frozen test-plan artifact (out of scope). No edits needed (per don't-churn resolution).

## 4. Guard & verify

- [x] 4.1 Added `TstDialingBlocks::shippedKb_editorModelAndRealProfileNames_guard` in `tests/tst_dialing_blocks.cpp` (links `ai.qrc` → reads `:/ai/profile_knowledge.md`). Asserts: (a) none of the 4 stale A-Flow names; (b) all 8 real built-in D-Flow/A-Flow titles present (authoritative set from §1.2); (c) no reintroduced profile-implying framing ("base D-Flow", "D-Flow (base)", "D-Flow/A-Flow variant", "D-Flow / Damian family"); (d) drift guard — 42 `## ` headings, 27 `Also matches:` lines, 4 load-bearing headers + the D-Flow alias line byte-stable. All assertions statically verified to hold against the edited KB.
- [x] 4.2 Built via Qt Creator MCP against the correct tree (`/Users/jeffreyh/Development/GitHub/Decenza`, after the user re-pointed Qt Creator from the separate `Decenza-Desktop` clone): **0 errors, 0 warnings**. Full suite: **2075 passed, 0 failed, 0 skipped, 0 warnings** (was 2074 → +1 is the new guard `shippedKb_editorModelAndRealProfileNames_guard`, confirmed pass at `tst_dialing_blocks.cpp:1832`). No functional change, as expected.
- [x] 4.3 Spot-read of the rendered D-Flow/A-Flow KB region (injected verbatim into both AI surfaces): D-Flow/A-Flow read as editor *types* with the explicit title-prefix rule; profiles are the slash-names; A-Flow names are the real built-ins; LRv2/LRv3 standalone & distinct from each other; Damian's Q correctly the Advanced-editor related oddball; no "variant/family/base D-Flow" framing; all headers + `Also matches:` lines visibly unchanged.
